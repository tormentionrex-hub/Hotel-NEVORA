// CLASE CLIENTE (SEGÚN TU IMAGEN)
// Define al huesped con nombre, identificacion y contacto.
class Cliente {
  constructor(nombreCompleto, identificacionPersonal, contactoEmail) {
    this.nombre = nombreCompleto;
    this.identificacion = identificacionPersonal;
    this.contacto = contactoEmail;
  }
}
